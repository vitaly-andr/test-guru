<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    <% flash.each do |key, message| %>
    <div class="flash <%= key %>">
      <%= message %>
    </div>
  <% end %>
  </body>
  <footer>
    <p>&copy; <%= current_year %> Vitaly Andrianov</p>
    <p>
      <%= link_to 'TestGuru', github_url('your-username', 'test-guru') %> - A simple application for managing tests.
    </p>
    <p>
      <%= link_to 'Учебный проект в онлайн-школе Thinknetica', 'https://thinknetica.com' %>
    </p>
  </footer>
</html>
