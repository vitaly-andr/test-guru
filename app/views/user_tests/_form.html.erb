<%= form_with model: user_test, local: true do |form| %>
  <% if user_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_test.errors.count, "error") %> prohibited this user test from being saved:</h2>
      <ul>
        <% user_test.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :test_id %>
    <%= form.number_field :test_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
