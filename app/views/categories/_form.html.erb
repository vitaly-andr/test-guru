<%= form_with model: @category do |form| %>
  <%= render 'shared/errors', resource: @category %>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
