<tr>
  <td><%= link_to question.body, admin_question_answers_path(question) %></td>
  <td class="actions">
    <%= link_to t('shared.actions.edit'), edit_admin_question_path(question), class: 'btn btn-sm btn-outline-secondary' %>
    <%= link_to t('shared.actions.delete'), admin_question_path(question), data: { turbo_method: :delete, turbo_confirm: t('shared.actions.confirm_delete') }, class: 'btn btn-sm btn-outline-danger' %>
  </td>
</tr>
