<h1><%= t('.header') %></h1>
<p>
  <strong><%= t('admin.users.index.email') %>:</strong>
  <%= @user.email %>
</p>
<p>
  <strong><%= t('admin.users.index.admin') %>:</strong>
  <%= @user.admin? ? 'Yes' : 'No' %>
</p>
<%= link_to t('shared.actions.edit'), edit_admin_user_path(@user), class: "btn btn-secondary" %>
<%= link_to t('.back_to_users'), admin_users_path, class: "btn btn-primary" %>
